# Add the following line to your server's configuration block
# include "/path/to/script/private/nginx.conf";

add_header X-Frame-Options "SAMEORIGIN";
add_header X-XSS-Protection "1; mode=block";
add_header X-Content-Type-Options "nosniff";

index index.html index.htm index.php;

charset utf-8;

location / {
    try_files $uri $uri/ /index.php?$query_string;
}

location = /favicon.ico { access_log off; log_not_found off; }
location = /robots.txt  { access_log off; log_not_found off; }

error_page 404 /index.php;

# Block every dot file/directory except the ones starting with .well-known
location ~ /\.(?!well-known).* {
    deny all;
}

# Block private directories
location ^~ /private {
   deny all;
   return 403; # return 403; # Can be removed
}

#location ~* ^/uploads/.*\.(php|phtml)$ {
#    deny all;
#    #return 403;
#    break;
#}

rewrite ^/uploads/.*\.(php|phtml)$ / last;
#location /uploads {
#    location ~ \.php$ {
#    return 403;
#    }
#}
